# 4.7.0 演进与优化大纲

## 核心目标
解决目前存在的遗留痛点（系统通知弹窗清理、模块解耦），并落实长期规划（重构与桌面化演进）。在 4.6.0 的基础上，对“系统通知”及相关强侵入的 UI 体验进行精简，进一步为后续引入真正的“桌面壳/托盘独立窗”铺路。

## 第一阶段：体验与通知治理（当前痛点解决）
* **关闭强干扰系统通知**：
  * 在 `web/src/App.vue` 等核心组件中，默认关闭 `startupCheckUpdate` 的弹窗。
  * 将 `ElNotification` 提醒降级或在设置里将默认值改为 false，避免非原生桌面时的突兀体验。
* **桌面端体验预留（系统通知过渡）**：
  * 将应用内的提示信息与未来计划做的桌面端通知区分开，不在网页侧做强阻断。

## 第二阶段：前端架构瘦身与解耦
* **根组件拆分与状态下沉**：
  * 目前 `App.vue` 承担了过多的非视图逻辑（如启动检查、Grok 环境嗅探、全屏检测等）。
  * 将全局嗅探逻辑抽取至专门的 `boot` 或 `composables/useAppInit.js` 中。
* **组件级解耦**：
  * 继续 3.8.1 的拆分工作，对 `Workbench.vue` 和 `App.vue` 的依赖引用进行依赖梳理，使用更清晰的 Provider/Inject 或者 Pinia Store 替代强绑定。

## 第三阶段：核心引擎重构（解耦与防御）
* **Engine 事件总线化**：
  * 将 `server/src/engine/` 中的强调用（直接写 `db`，直接触发 `bus.js` 广播）改为事件派发器 (`EventEmitter` 模式)。
  * 使得状态流转 (`advance`, `gates`) 仅仅产生“副作用描述”，由单一监听者进行统一存储和通知，规避并发写冲突和偶发的“事件漏发”。
* **依赖注入设计**：
  * 对 `runners.js`（终端执行与管线适配）和 `terminalService.js` 进行重构，引入依赖注入模式，以支持更好的单元测试及后续的“多终端标签隔离”。

## 第四阶段：桌面化基础搭建 (铺垫)
* **API 与生命周期对接**：
  * 为 Electron / Tauri 或系统级壳准备好握手 API，支持系统托盘的最小化、系统原生通知。
  * 规划 `ACW_DESKTOP_ENV` 环境变量探测，平滑兼容现有的 Web 版与未来的壳版本。
