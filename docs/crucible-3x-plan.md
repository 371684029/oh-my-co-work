# oh-my-co-work 3.x · 熔炉实施计划

| 属性 | 内容 |
|------|------|
| 目标版本 | `3.x` |
| 状态 | **3.2 情境注入已交审** |
| 更新日期 | 2026-08-18 |
| 设计文档 | [crucible-3x.md](./crucible-3x.md) |
| 前置 | `2.x` 封板后再启动；JSONL Adapter 契约保持稳定 |

版本号表示功能边界，不要求 Grok 或熔炉桌面形态在 `3.0` 一次完成。

---

## 1. 实施原则

1. 入口是 **精灵**（黑裙 Logo），不是按钮；先本机 Grok TUI，再客户端。  
2. 用户只有 **是否适配**；内部优先改代码，改不了再步骤标记。  
3. 改源文件前默认压缩包；标记不改会话气泡种类。  
4. 产品层 **统一管理员全部改称熔炉**；暂定只 Grok。  
5. 未配置也不隐藏精灵，提示下载并配置 Grok Build。  
6. JSONL 契约不改；流程轨用 Tab 标适配步骤。  
7. 只做极简 Agent；桌面就是精灵。

---

## 2. 版本切分

| 版本 | 主题 | 交付边界 |
|------|------|----------|
| `3.0` | 熔炉替换管理员 + 精灵入口 | 展示名/文案：统一管理员 → 熔炉；加人/加步骤仅「是否适配」+ hover；精灵三态；未配置提示装 Grok；可先嵌 Grok TUI |
| `3.1` | 适配落地 | 优先改代码（先打压缩包）；失败则步骤标记；流程轨 Tab；会话原样展示；三套角色 prompt |
| `3.2` | 工作流 prompt 工程 | 节点一览 + 当前节点合同；角色壳跟游标换挡；详见 [crucible-3.2.md](./crucible-3.2.md) |
| `3.3` | 桌面精灵 | 托盘随叫随到的熔炉；仍无内置工具 |
| 后置 | Grok 客户端 | 从本机 TUI 拓到 Grok 客户端；原 3.2 主题后移 |

`3.0` 就要能看见精灵，不要做空按钮。未接 Grok 时允许只提示配置。

---

## 3. Phase 0：收口（文档已做，代码未做）

- [x] 写下设计：[crucible-3x.md](./crucible-3x.md)  
- [x] 写下本计划  
- [ ] 2.x 工具卡 / 多终端等补齐合入后再开 3.0 代码  
- [ ] 选定「改不了代码」的判定规则  
- [x] 选定压缩包落盘位置  
- [x] 选定流程轨 Tab 的 UI（角标 vs 筛选页）  
- [ ] `unified_admin` 内部 id 是否保留

---

## 4. Phase 1：3.0 可配置的熔炉

### 4.1 产品

- [x] 产品文案与默认成员展示：统一管理员 → 熔炉（内部 key 可暂留）。  
- [x] 加人 / 加步骤：**是否适配** 单选项 + hover（优先改代码，否则标记）。  
- [x] 黑裙精灵入口（闲置 / 工作 / 等人），不是按钮。  
- [x] 未配置 Grok：精灵仍在，提示下载 Grok Build 并配置。  
- [x] 可先在内嵌终端跑本机 Grok TUI。  
- [x] 闸门文案：管理员确认 → 熔炉（通道仍可配）。

### 4.2 暂不做

- 不给用户两个适配入口。  
- 不隐藏未配置的精灵。  
- 不新开适配用 `step_type`。  
- 不在熔炉内做工具列表。

### 4.3 验收

- [x] 不选熔炉时，加成员 / 建群 / 闸门与 2.x 行为一致。  
- [x] 关掉熔炉成员后，已存在的群和脚本成员仍能跑。  
- [x] 配成熔炉的闸门不会绕过节点状态机。

---

## 5. Phase 2：3.1 适配落地

- [x] 勾选适配后：**先改代码**；改前打压缩包。  
- [x] 改不了则给该步骤打 **适配标记**（沿用原 step_type）。  
- [x] 会话框按原消息类型展示。  
- [x] 流程轨为带标记步骤加 Tab/标识。  
- [x] 尽量不影响源文件其它使用场景。  
- [x] 能接到 JSONL 的优先写侧通道。  
- [x] 三套作用各一份 prompt + 记忆：成员适配 / 节点适配 / 系统审核；本轮只装 `ACTIVE.md`。

验收：开适配后用户不必选择路径；能改代码的成员有备份包；不能改的步骤在流程轨 Tab 里能找到，聊天形态不变。适配步与闸门不共用同一份记忆。

---

## 6. Phase 3：3.2 工作流 prompt 工程

设计全文：[crucible-3.2.md](./crucible-3.2.md)。原「Grok 客户端 / Build 式编码」后置，不挡开聊懂事。

- [x] 开聊编译 **节点一览**（类型/执行者/适配/闸门/状态/游标）+ 当前节点合同。
- [x] 角色壳跟当前节点标记换，不跟业务场景名换。
- [x] 适配步、熔炉闸门换壳时地图留下。
- [x] **开始执行后**游标每动一格就刷新当前节点合同（开跑 / 等人 / 完成），不灌聊天。
- [ ] 精灵点开时是否切主持（后置）。
- [ ] `step.hint` 人给某一格的短备注（后置，不作第二套编排）。

验收：开聊后 `SITUATION.md` 能看出每个节点是什么、游标在哪；不依赖某个业务场景词。

### 6.1 后置：Grok Build 式编码

- [ ] 设置中配置提供方与密钥（本机，不进 JSONL / 群报告）。  
- [ ] 熔炉「写代码 / 构建」任务启动 PTY 中的约定执行体，不直连一套无进程治理的沙箱。  
- [ ] 无 Grok 时不隐藏精灵，只提示下载配置。  
- [ ] 厂商暂定 Grok，不做切换 UI。

验收：一次「改文件 + 跑构建」在工作台终端可见、可停止、可进节点结果。

开放：Grok Build 具体安装包与配置项以当时官方为准。

---

## 7. Phase 4：3.3 熔炉桌面

- [ ] 独立小窗或托盘入口，产品名「熔炉」，随叫随到。  
- [ ] 角色标三态：闲置 / 工作 / 等人。  
- [ ] 桌面就是精灵；极简 Agent。  
- [ ] 可切换回完整工作台（三栏 + 内嵌 TUI）。  
- [ ] 熔炉桌面内加人 / 建群 / 过闸门走同一 API。  
- [ ] 不维护第二份 SQLite。  
- [ ] 通知（构建完、闸门等人）后置可做，但不得替代闸门 UI。

验收：只开熔炉精灵能完成一次「是否适配」；切回工作台能看到同一群、同一终端。没有工具箱。

---

## 8. 测试与安全（随 3.0 起）

- 熔炉闸门与人闸门同一套 `waiting_human` / 完成语义（或显式新状态，但不可无记录推进）。  
- 熔炉不能提交任意 shell 绕过成员白名单（沿用 2.x：只跑批准的执行体）。  
- 云端请求不把本机 Token、Adapter 原文、完整密钥写入聊天默认可见字段。  
- 熔炉桌面与浏览器工作台同一 Origin / 本机令牌模型。

---

## 9. 2.x 仍后置、不因 3.x 提前吞掉的

专用 Claude/Codex Adapter、服务重启接管 PTY、L3 工作流协同、条件分支并行。熔炉 3.1 用包装脚本 + JSONL 覆盖「多数 CLI」，不替代专用解析器需求时的另案。

---

## 10. 3.x 完成定义（封板口径，未到日期）

同时满足才可称 3.x 主题落地：

- 加人/加步骤仅「是否适配」；精灵入口；管理员文案改为熔炉。  
- 优先改代码（先备份包），否则步骤标记；会话原样、流程轨 Tab。  
- 未配置 Grok 仍展示精灵并提示安装配置。  
- 三态卡通 Logo。暂定只 Grok。只做极简桌面精灵。
