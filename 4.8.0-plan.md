# 4.8.0 Grok & Cursor Dual-Agent TUI Optimization Plan
(4.8.0 桌面端优先 Grok & Cursor 双 Agent TUI 体验与视觉深度优化计划)

## 核心目标
解决当前 Grok Build 界面美观度不足、终端交互生硬、桌面端 Native 体验未打通的问题，并扩展支持 **Cursor CLI** 成为熔炉双 Agent。
按照用户明确要求：
1. **优先保证 Desktop（Electron）使用与极致体验，Web 端能用即可**。
2. **隐藏/暂不开发 Grok GUI 模式（专心做好纯粹终端），暂不处理网页特有交互**。
3. **熔炉增加 Cursor CLI 支持，实现 Grok CLI + Cursor CLI 双 Agent 并存，顶栏增加 Tab 选项供用户自由切换**。
4. **提供进程后台常驻与页面无感切换机制**。

---

## 🤖 1. 熔炉双 Agent 架构 (Grok CLI + Cursor CLI Dual-Agent Tabs)

- **顶栏 Agent 切换 Tab 栏 (Top Agent Tab Bar)**
  - 熔炉顶栏提供直观的 Tab 切换卡：`Grok CLI (Grok Build)` 与 `Cursor CLI (Cursor Agent)`。
  - 支持快捷键 (`Cmd+1` / `Cmd+2` 或 `Ctrl+Tab`) 快速在双 Agent TUI 之间无缝切换。
- **独立进程与会话隔离 (Isolated Agent PTY Sessions)**
  - Grok 与 Cursor 分别运行在各自独立的 PTY 常驻进程（如 `grok` 与 `cursor` / `cursor-agent` CLI 命令）中，各自拥有独立的工作路径与 Buffer 缓冲，互不干扰。
- **配置与探测 (CLI Detection & Configuration)**
  - 系统扩展 `grokStatus.js` / `slashCommands.js`，增加对 `cursor` CLI 命令的探查与版本探测；未安装时给出现成安装提示。

---

## ⚡️ 2. Agent 进程后台常驻与页面切换机制 (Background Persistence & Seamless Re-attach)

### Q: 切换页面或切换 Agent Tab 后，进程会断开吗？

**答：双 Agent 进程在后台均为 100% 常驻，切页面或切 Tab 绝不断开！**

#### 1. 服务端进程常驻机制 (Server Process Persistence)
- **后端常驻（node-pty 常驻守护）**：Grok CLI 与 Cursor CLI 分别运行在服务端的 PTY 进程池中。用户切页面或在顶栏切换 Grok / Cursor Tab 时，**后台的两条 Agent 进程均在后台正常运行、思考和执行命令**，绝对不会被杀死或丢失上下文。
- **只有显式动作才会杀死对应 Agent**：只有在顶栏主动点「关闭熔炉」或「新开熔炉」时，才会结束或重建对应 Agent 进程。

#### 2. 前端 4.8.0 无感重连与后台守护 (Frontend Keep-Alive)
- **Keep-Alive 终端视口常驻 (Terminal Viewport Keep-Alive)**：使用组件 `keep-alive`，切换 Tab 或切页面时**仅隐藏 DOM，不断开 WebSocket 字节流**。
- **后台完成提示 (Background Execution Indicator)**：非激活状态的 Agent（例如当前在 Grok 页，Cursor 在后台完成思考）在 Tab 上显示**“亮色提示点”**并触发桌面系统通知。

---

## 🌐 3. HTML Web 端最低兼容策略 (Web Minimum Fallbacks: 能用即可)

| 功能模块 | Electron 桌面端原生行为 (优先保证) | HTML / Web 网页端降级策略 (能用即可) |
| :--- | :--- | :--- |
| **Grok & Cursor 双 Agent 常驻** | 100% 服务端 Node 常驻（切窗口/切页面/切 Tab 不停） | **完全一致**：走 Node 服务端 PTY 进程常驻，双 Agent 均不断开。 |
| **TUI 视觉美化与双 Tab 切换** | 沉浸式无边框 Tab 栏 | **基础质感 Tab**：支持 Grok / Cursor 双 Tab 切换与高对比度 xterm 主题。 |
| **xterm 插件与快捷键** | 完全支持 (`Cmd+F` 搜索 / 点击 URL) | **支持**：纯 JS 搜索与超链接功能。 |
| **原生终端回滚 (Scrollback)** | 完整支持 (PTY Buffer 平滑锁底) | **支持**：共享 xterm.js 内存 Buffer 滚屏体验。 |
| **GUI 入口隐藏 / 纯 TUI 专注** | 默认隐藏 GUI，专精双 Agent TUI | **一致**：网页端同步隐藏 GUI。 |
| **Native Shell 直连** | 允许 Electron 进程直拉本地 Shell/进程 | **降级 (WebSocket PTY)**：Web 走 Node 服务跑 CLI PTY 命令。 |
| **文件拖拽 (Drag & Drop)** | 读取本地绝对路径直接注入光标 | **降级 (Inbox 上传)**：自动上传至 `data/furnace/inbox/` 并填入相对路径。 |
| **桌面系统通知与托盘** | Electron IPC 原生通知 / Tray 最小化 | **降级 (ElMessage)**：网页端轻量 Toast 轻提示。 |

---

## 🛠 重点改进领域

### 🎨 1. 视觉美化与终端渲染 (Visual Aesthetics)
- **精致沉浸式桌面外壳与双 Tab (Native Window, Frame & Tabs)**
  - 桌面端无边框沉浸式窗口，顶部融合 Mac 一体化 Control Points / Windows 标题栏与 Grok/Cursor 双 Tab 卡片。
  - 背景模糊/微光磨砂效果（Backdrop-Filter / Vibrancy）。
- **主题色彩与字体排版优化 (Typography & Custom Themes)**
  - 精调终端对比度与 ANSI 颜色映射（Grok 经典绿/灰主题与 Cursor 极客紫/暗色主题区别）。
  - 默认加载等宽编程字体（Cascadia Code / JetBrains Mono / SF Mono），优化行高 (`1.35`) 与字距，支持 Cursor Glowing。

---

### ⌨️ 2. 桌面端快捷键与终端 Addon 增强 (Desktop Shortcuts & Terminal Addons)
- **快捷键无缝绑定 (Native Keyboard Shortcuts)**
  - 支持 `Cmd+1 / Cmd+2` 快速切换 Agent Tab，`Cmd+C / Ctrl+C`（选中文本复制 / 无选中 SIGINT）、`Cmd+V / Ctrl+V`（无缝粘贴）。
  - `Cmd+F / Ctrl+F` 实时搜索终端文本、`Cmd+K / Ctrl+K` 快速清屏、`Cmd+R` 重启当前 Agent。
- **集成 xterm 功能插件 (xterm.js Addons Integration)**
  - 集成 `@xterm/addon-search` 搜索插件与 `@xterm/addon-web-links` 超链接插件。
- **原生终端式历史回滚与平滑锁底 (Native Scrollback Experience)**
  - 支持鼠标滚轮、`Shift+PageUp / Shift+PageDown` 在 PTY 缓冲集中上下回滚查看历史，向上滚动暂停锁底，滑回底部自动跟随。

---

### 🖱 3. 原生 Shell 接入与拖拽快捷交互 (Native Shell & Floating Controls)
- **直连 Native Shell 运行 `grok` 与 `cursor`**
  - 允许客户端直拉本地 Shell 运行 CLI 命令，去除非必要胶水层。
- **原生文件拖拽与路径注入 (File Drag & Drop)**
  - 允许拖拽本地文件/文件夹进入当前 TUI，自动转化为相对/绝对路径并插入光标处。

---

## 🗓 实施阶段与路线图 (Phases Roadmap)

### Phase 1: 双 Agent Tab 架构、GUI 隐藏与视觉外壳 (Dual-Agent Tabs & Visual Polish)
1. 顶部新增 Grok CLI 与 Cursor CLI 双 Agent Tab 选择器，默认隐藏 GUI 入口。
2. 扩展后端 `terminalService` 支持 Cursor CLI 探查与双常驻进程管理。
3. 优化 `TerminalView.vue` 样式、支持 Electron 沉浸式标题栏与 Tab 切换。

### Phase 2: 后台常驻无感切页/Tab 与 Terminal Addons (Persistence & Addons)
1. 前端实现 Keep-Alive 与双 PTY 状态托管，切页面与切 Agent Tab 时保持 WS 连接不断。
2. 增加后台生成提示（Agent Tab 亮点提示 + 桌面通知）。
3. 集成 `@xterm/addon-search` 与 `@xterm/addon-web-links`，优化原生 Scrollback 回滚。

### Phase 3: 本地 Native Shell 直连与拖拽交互 (Native Shell & Drag & Drop)
1. 优化桌面端 Shell 直连/脚本拉起机制，直接运行 `grok` 与 `cursor` CLI 命令。
2. 增加桌面端文件拖拽至 TUI 自动转路径功能及悬浮快捷操作栏。
3. 实现 Web 端平滑降级逻辑。

### Phase 4: 测试与体验验收 (Verification & Polish)
1. 验证双 Agent 切换、原生终端历史翻阅、后台常驻切页、Shell 执行与 Web 降级。
2. 完整执行 `npm run lint` 和单元测试，确保无回归问题。

---

## 🏁 验收标准 (Acceptance Criteria)
1. **双 Agent 并存**：顶栏提供 Grok / Cursor 双 Tab，可自由无缝切换，各自常驻独立 PTY 进程。
2. **Desktop 优先&后台常驻**：优先保证 Electron 桌面体验；切页面、切窗口、切 Agent Tab 时后台进程绝对不断开。
3. **纯粹 TUI 聚焦**：默认隐藏 GUI 入口，全量集中打造极简极客双 Agent TUI 体验。
4. **视觉美观与原生历史**：现代 Terminal 质感，滚轮与快捷键 100% 还原原生 PTY Buffer 回滚。
5. **桌面深度整合与 Web 最低兼容**：桌面端支持直连 Native Shell 运行与原生拖拽；Web 端降级能用即可。
6. **测试无误**：所有代码规范与现有单元测试/Web测试全面通过。
