# 4.8.0 Grok TUI & Desktop Experience Optimization Plan
(4.8.0 桌面端优先 Grok TUI 体验与视觉深度优化计划)

## 核心目标
解决当前 Grok Build 界面美观度不足、终端交互生硬、桌面端 Native 体验未打通的问题。
按照用户要求：**优先完善 Grok Build 的 TUI，桌面端（Electron）优先，隐藏/暂不开发 Grok GUI 模式（专心做好纯粹终端），暂不处理网页特有交互**。同时提供清晰的 **HTML/Web 端平滑降级与兼容策略**。

---

## 🌐 HTML Web 端支持分析与降级处理策略 (Web Compatibility & Fallbacks)

### Q: HTML/Web 网页端能支持吗？不支持怎么处理？

| 功能模块 | Electron 桌面端原生行为 | HTML / Web 网页端兼容与降级处理方案 |
| :--- | :--- | :--- |
| **TUI 视觉美化与主题** | 完全支持（含窗口无边框与微光磨砂背景） | **完全支持**：xterm 高对比度主题、Cascadia/JetBrains Mono 编程字体、圆角边框与发光状态胶囊均完全通用。 |
| **xterm 插件与快捷键** | 完全支持 (`Cmd+F` 搜索 / 点击 URL) | **完全支持**：搜索插件 (`@xterm/addon-search`)、超链接插件 (`@xterm/addon-web-links`) 均属于 Web 前端纯 JS 能力，网页端 100% 支持。 |
| **原生终端回滚 (Scrollback)** | 完全支持 (PTY Buffer 平滑锁底) | **完全支持**：基于 xterm.js 内存 Buffer 滚屏，Web 端与 Desktop 共享完全一致的滚轮与 `Shift+PageUp/Down` 查看历史体验。 |
| **GUI 入口隐藏 / 纯 TUI 专注** | 完全支持 | **完全支持**：前端统一默认隐藏 GUI 面板，Web 端与桌面端均直接展示极简 TUI 视口。 |
| **Native Shell 直连** | 允许 Electron 进程直拉本地 Shell/进程 | **平滑降级 (WebSocket PTY)**：Web 浏览器受安全沙箱限制，无法直接调动本机 Shell，继续通过 WebSocket (`ws://`) 连接本机的 Node 服务 (`terminalService`) 托管跑 `grok` PTY 命令，体验完全无缝。 |
| **文件拖拽 (Drag & Drop)** | 读取本地绝对路径直接注入光标 | **平滑降级 (Inbox 上传 / 相对路径)**：网页端因浏览器安全限制读取不到绝对路径，拖拽文件时自动上传至 `data/furnace/inbox/`，并填入相对路径 `inbox/filename.png` 到终端。 |
| **桌面系统通知与托盘** | Electron IPC 原生通知 / Tray 最小化 | **平滑降级 (Web Notification / ElMessage)**：网页端优先申请 Web `Notification` 权限；若未授权则降级为温和的页面顶栏 Toast (`ElMessage.info`) 轻提示。 |

---

## 🛠 重点改进领域

### 🎨 1. 界面隐藏与 TUI 专属呈现 (GUI Hiding & TUI Focus)
- **隐藏 Grok Build GUI 面板入口 (Hide Grok GUI Surface)**
  - 暂时隐藏/禁用 GUI 对话框界面与顶栏 GUI 切换按钮，打开 Grok 熔炉时默认直接进入无界 TUI 终端模式。
  - 减少复杂的双模式切换负担，将全部交互集中于极高质感的 TUI 终端本身。

### 🎨 2. 视觉美化与终端渲染 (Visual Aesthetics)
- **精致沉浸式桌面外壳 (Native Window / Frame Styling)**
  - 桌面端引入无边框沉浸式窗口结构（Mac 顶部一体化 Control Points / Windows 自定义精简标题栏与状态徽标）。
  - 配合背景模糊/微光磨砂效果（Backdrop-Filter / Vibrancy），提升整体极客与现代化质感。
- **Grok 主题色彩与字体排版优化 (Typography & Custom Themes)**
  - 精调终端对比度与 ANSI 颜色映射（亮色高亮、深色底色、语法与输出色调区分），避免灰暗杂乱。
  - 默认加载高质量等宽编程字体（Cascadia Code / JetBrains Mono / SF Mono），优化行高 (`lineHeight: 1.35`) 与字距，支持 Cursor Glowing (光标微光呼吸动画)。
- **终端区域边框与状态指示 (Status Pill & Layout)**
  - 给 xterm 容器增加圆角内边距、微弱发光边框与动态状态胶囊（如 `Grok 进程在线` / `CPU/Memory 占用` / `cwd 路径`）。

---

### ⌨️ 3. 桌面端快捷键与终端 Addon 增强 (Desktop Shortcuts & Terminal Addons)
- **桌面原生快捷键无缝绑定 (Native Keyboard Shortcuts)**
  - 修复 `Cmd+C / Ctrl+C`（选中文本时复制、无选中时发 SIGINT）与 `Cmd+V / Ctrl+V`（原生剪贴板无缝粘贴）。
  - 增加 `Cmd+F / Ctrl+F` 快速唤起终端内搜索框、`Cmd+K / Ctrl+K` 快速清屏、`Cmd+R` 快速重启/重开 Grok 熔炉进程。
- **集成 xterm 功能插件 (xterm.js Addons Integration)**
  - 引入 `@xterm/addon-search`：支持在 TUI 大段输出中实时搜索关键字、高亮匹配项与上下一项导航。
  - 引入 `@xterm/addon-web-links`：使 Grok 在终端中输出的 URL/文件路径变成可直接点击跳转的原生超链接。
- **原生终端式历史回滚与平滑锁底 (Native Scrollback Experience)**
  - 彻底打通原生终端滚屏体验：无需依赖外部侧边抽屉，直接支持鼠标滚轮、`Shift+PageUp / Shift+PageDown` 在 xterm PTY 缓冲区中无缝上下回滚查看历史。
  - 智能锁底机制：用户向上滚动查阅历史时自动暂停锁底，滑回底部自动恢复跟随。

---

### 🖱 4. 桌面端原生 Shell 接入与拖拽快捷交互 (Native Shell & Floating Controls)
- **直连内嵌 Native Shell 运行 `grok` (Direct Native Shell Execution)**
  - 允许客户端直接拉起本地 Shell/远程 Shell 脚本无缝跑 `grok` 命令，去除非必要的 Web/GUI 胶水层，提供纯粹的原生终端交互。
- **原生文件拖拽与路径注入 (File Drag & Drop to Terminal)**
  - 允许拖拽本地文件/文件夹直接进入 Grok TUI 视图，桌面端自动转换为相对/绝对路径并插入当前终端输入光标处。
- **悬浮控制浮条 (Floating Action Overlay)**
  - 在 TUI 右上角/底部提供轻量级悬浮操作面板：模型切换提示、一键清屏、快捷指令模板按钮、状态重置等。
- **桌面 Native IPC 协同 (Desktop Notification & Tray Integration)**
  - Grok 长时间任务/思考完成后，通过 Electron 原生系统通知提示用户。

---

### 📜 5. 历史记录与上下文查看重构 (Native History & Context Experience)
- **原生终端内查阅历史 (Native Terminal Scrollback)**
  - 将历史查看主体验回归原生 TUI 本身（与原生 Terminal / tmux 交互完全一致），利用 PTY 原生 Buffer 与 xterm 缓冲记录，避免切出界面或使用臃肿 UI。
- **结构化上下文辅助 (Optional Structured Context View)**
  - 对于需要在终端外对比的大段思考过程/结构化节点，提供极轻量弹窗或快捷键触发浮层，保持终端主视口的纯净。

---

## 🗓 实施阶段与路线图 (Phases Roadmap)

### Phase 1: 视觉风格、GUI 隐藏与外壳重构 (GUI Hide & Visual Polish)
1. 默认隐藏 Grok Build GUI 模式入口，专精 TUI 模式呈现。
2. 优化 `TerminalView.vue` 样式，精调主题颜色、字体、光标与圆角边距。
3. 增加桌面端 Electron 无边框/一体化顶部控制栏，赋予 TUI 深度沉浸感。

### Phase 2: 终端 Addons、原生滚屏与快捷键 (Shortcuts & Native Scrollback)
1. 在 `TerminalView.vue` 中集成 `@xterm/addon-search` 与 `@xterm/addon-web-links`。
2. 优化 xterm PTY scrollback 机制，实现 100% 还原原生终端的滚轮与 `Shift+PageUp/Down` 历史翻阅体验。
3. 规范桌面端快捷键系统 (`Cmd/Ctrl+C/V/F/K/R`)，改善剪贴板与搜索交互。

### Phase 3: 本地 Native Shell 直连与拖拽交互 (Native Shell & Drag & Drop)
1. 优化桌面端 Shell 直连/脚本拉起机制，直接运行 `grok` CLI 命令。
2. 增加桌面端文件拖拽至 TUI 自动转路径功能及悬浮快捷操作栏。
3. 实现 HTML/Web 端拖拽落入 `inbox/` 填入相对路径的平滑降级逻辑。

### Phase 4: 测试与体验验收 (Verification & Polish)
1. 验证原生终端历史翻阅、Shell 执行、拖拽与 Web 降级兼容性。
2. 完整执行 `npm run lint` 和单元测试，确保无回归问题。

---

## 🏁 验收标准 (Acceptance Criteria)
1. **纯粹 TUI 聚焦**：默认隐藏 GUI 入口，全量集中打造极简极客 TUI 体验。
2. **视觉美观**：TUI 视觉效果达到现代桌面 Terminal (如 Warp / Cursor / VSCode) 级别质感，字体清晰，主题饱满。
3. **原生历史体验**：查看历史与滚屏体验同原生 Terminal 完全一致，支持滚轮/快捷键无缝回滚 PTY Buffer。
4. **交互流畅**：快捷键 `Cmd/Ctrl+F` 可流畅搜索终端文本，URL/文件路径可点击，复制粘贴无卡顿。
5. **桌面深度整合与 Web 降级**：桌面端支持直连 Native Shell 运行 `grok` 与原生拖拽路径；Web 端自动平滑降级走 WebSocket PTY 与 Inbox 相对路径，双端均可稳定使用。
6. **测试无误**：所有代码规范与现有单元测试/Web测试全面通过。
